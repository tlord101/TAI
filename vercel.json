{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/((?!assets/|favicon.ico|sw.js).*)",
      "destination": "/index.html"
    }
  ]
}